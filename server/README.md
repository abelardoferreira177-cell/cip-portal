# API - Consulta de Certificados (CIP)

## Rodar local
1. Copie `.env.example` para `.env` e ajuste usuário/senha.
2. `npm install`
3. `npm run seed` (opcional - cria registros de exemplo)
4. `npm run dev`

A API roda em `http://localhost:3001`
